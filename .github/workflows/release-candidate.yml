name: Release Candidate

on:
  workflow_dispatch:
    inputs:
      require_hw_p0:
        description: "Require hardware P0 artifact in this RC run"
        type: boolean
        default: false

jobs:
  branch-guard:
    runs-on: ubuntu-22.04
    steps:
      - name: Validate release branch
        shell: bash
        run: |
          set -euo pipefail
          ref="${GITHUB_REF_NAME}"
          if [[ "$ref" != release/* ]]; then
            echo "This workflow must run from release/* branches. Current: $ref" >&2
            exit 1
          fi

  rc-linux:
    needs: branch-guard
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Configure
        run: |
          cmake -S . -B build \
            -DOB_BUILD_TESTS=ON \
            -DOB_BUILD_EXAMPLES=OFF \
            -DOB_BUILD_TOOLS=OFF \
            -DOB_BUILD_DOCS=OFF

      - name: Build test targets
        run: cmake --build build --target ob_core_nohw_test ob_playback_smoke_test -j

      - name: Run no-hardware regression and emit JUnit
        shell: bash
        run: |
          set -uo pipefail
          mkdir -p reports

          CORE_RC=0
          PLAYBACK_RC=0

          ./build/tests/core_nohw_test/ob_core_nohw_test || CORE_RC=$?

          BAG_PATH=$(find tests/rosbag -maxdepth 1 -name '*.bag' | head -n 1)
          if [ -z "$BAG_PATH" ]; then
            echo "No .bag file found in tests/rosbag" >&2
            PLAYBACK_RC=2
          else
            ./build/tests/playback_test/ob_playback_smoke_test "$BAG_PATH" || PLAYBACK_RC=$?
          fi

          python3 - <<'PY'
          import xml.etree.ElementTree as ET

          core_rc = int("${CORE_RC}")
          playback_rc = int("${PLAYBACK_RC}")

          suite = ET.Element(
              "testsuite",
              name="rc_linux_nohw",
              tests="2",
              failures=str(int(core_rc != 0) + int(playback_rc != 0)),
              errors="0",
              skipped="0",
          )

            case_core = ET.SubElement(suite, "testcase", classname="rc_linux.p0", name="core_nohw_p0")
          if core_rc != 0:
              fail = ET.SubElement(case_core, "failure", message=f"exit code {core_rc}")
              fail.text = "ob_core_nohw_test failed"

            case_playback = ET.SubElement(suite, "testcase", classname="rc_linux.p0", name="playback_smoke_p0")
          if playback_rc != 0:
              fail = ET.SubElement(case_playback, "failure", message=f"exit code {playback_rc}")
              fail.text = "ob_playback_smoke_test failed"

          root = ET.Element("testsuites")
          root.append(suite)
          ET.ElementTree(root).write("reports/rc_linux_junit.xml", encoding="utf-8", xml_declaration=True)
          PY

          if [ "$CORE_RC" -ne 0 ] || [ "$PLAYBACK_RC" -ne 0 ]; then
            exit 1
          fi

      - name: Upload rc-linux reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rc-linux-report-${{ github.run_id }}
          path: reports/rc_linux_junit.xml
          retention-days: 30

  rc-docs-check:
    needs: branch-guard
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run docs checks
        run: bash scripts/run_docs_checks.sh

  rc-examples-linux:
    needs: branch-guard
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run examples smoke
        run: bash scripts/run_examples.sh

  rc-windows-build:
    needs: branch-guard
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure
        run: |
          cmake -S . -B build `
            -DOB_BUILD_TESTS=ON `
            -DOB_BUILD_EXAMPLES=OFF `
            -DOB_BUILD_TOOLS=OFF `
            -DOB_BUILD_DOCS=OFF

      - name: Build
        run: cmake --build build --config Release

  rc-hw-p0:
    needs: branch-guard
    if: ${{ inputs.require_hw_p0 == true }}
    runs-on: [self-hosted, linux, sdk, usb, gemini-335]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate hardware P0 suite presence
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -d "tests/hardware" ]; then
            echo "Hardware P0 suite is required but tests/hardware is missing." >&2
            exit 1
          fi

      - name: Run hardware P0 pytest and emit JUnit
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p reports
          python3 -m pytest tests/hardware -m "p0 and hardware" -v \
            --junit-xml=reports/rc_hw_p0_junit.xml

      - name: Upload rc-hw-p0 report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rc-hw-p0-report-${{ github.run_id }}
          path: reports/rc_hw_p0_junit.xml
          retention-days: 30

  rc-summary:
    needs: [rc-linux, rc-windows-build, rc-docs-check, rc-examples-linux, rc-hw-p0]
    runs-on: ubuntu-22.04
    environment: rc-validation
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download report artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: "*report*"
          path: all_reports/
          merge-multiple: true

      - name: Run RC numeric policy gate
        run: |
          EXTRA_ARGS=""
          if [ "${REQUIRE_HW_P0}" = "true" ]; then
            EXTRA_ARGS="--require-hw-p0"
          fi

          python3 scripts/rc_policy_gate.py \
            --reports-dir all_reports \
            --min-success-rate 98.0 \
            --benchmark-baseline scripts/benchmark_baseline.csv \
            --benchmark-current all_reports/rc_benchmark_current.csv \
            --job-results "${JOB_RESULTS}" \
            ${EXTRA_ARGS} \
            --output-json reports/rc_gate_result.json \
            --output-md reports/rc_gate_result.md
        env:
          JOB_RESULTS: ${{ needs.rc-windows-build.result }},${{ needs.rc-docs-check.result }},${{ needs.rc-examples-linux.result }},${{ needs.rc-hw-p0.result }}
          REQUIRE_HW_P0: ${{ inputs.require_hw_p0 }}

      - name: Upload RC gate artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rc-policy-gate-${{ github.run_id }}
          path: |
            reports/rc_gate_result.json
            reports/rc_gate_result.md
          retention-days: 90

      - name: Generate release summary
        run: |
          echo "RC validation finished." >> "$GITHUB_STEP_SUMMARY"
          echo "- Linux build/no-hardware regression: ${{ needs.rc-linux.result }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Windows build: ${{ needs.rc-windows-build.result }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Docs check: ${{ needs.rc-docs-check.result }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Examples smoke (Linux): ${{ needs.rc-examples-linux.result }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          cat reports/rc_gate_result.md >> "$GITHUB_STEP_SUMMARY"
